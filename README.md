# RyChat

I've created a simple chatroom app using Django/Python for the backend and JavaScript/Ajax for some frontend tools along with HTML and CSS.

When starting a django project it is important to run the 
`django-admin startproject <name_of_project>` command.
